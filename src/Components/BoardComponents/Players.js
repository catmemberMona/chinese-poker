import React from 'react';

const Players = () => {
  return <div style={styles.players}></div>;
};

let styles = {
  players: {
    flex: 1,
    // justifyContent: 'start',
    borderColor: 'red',
  },
};

export default Players;
