import React from 'react';

const Footer = () => {
  return (
    <div style={styles.footer}>

    </div>
  )
};

let styles = {
  footer: {
    flex: 1,
    justifyContent: "center"
  }
} 

export default Footer